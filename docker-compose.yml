name: user-platform

services:
  postgres:
    build: ./postgres
    image: ahceneaiti/postgresql-user:latest
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app -d app"]
      interval: 5s
      timeout: 5s
      retries: 10

  kafka:
    build: ./kafka
    image: ahceneaiti/kafka-user:latest
    ports:
      - "9092:9092"
    volumes:
      - kafkadata:/var/lib/kafka/data

  mailhog:
    image: mailhog/mailhog:v1.0.1
    ports:
      - "1025:1025"
      - "8025:8025"

  kafka-ui:
    image: kafbat/kafka-ui:latest
    depends_on:
      kafka:
        condition: service_started
    environment:
      KAFKA_CLUSTERS_0_NAME: user-platform
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: "kafka:9092"
      DYNAMIC_CONFIG_ENABLED: "true"
    ports:
      - "8090:8080"

  user-service:
    build: ./user-service
    image: ahceneaiti/php-api-user:latest
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_started
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      APP_SECRET: local-dev-not-secret
      DATABASE_URL: "postgresql://app:app@postgres:5432/app?serverVersion=16&charset=utf8"
      KAFKA_BROKERS: "kafka:9092"
      KAFKA_TOPIC: "user-events"
      MAILER_DSN: "smtp://mailhog:1025"
    ports:
      - "8080:8000"
    restart: unless-stopped
    command:
      - sh
      - -c
      - >
        php bin/console doctrine:migrations:migrate --no-interaction --all-or-nothing &&
        exec php -d variables_order=EGPCS -S 0.0.0.0:8000 -t public public/index.php

  notification-service:
    build: ./notification-service
    image: ahceneaiti/notification-service:latest
    depends_on:
      kafka:
        condition: service_started
      mailhog:
        condition: service_started
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      APP_SECRET: local-dev-not-secret
      KAFKA_BROKERS: "kafka:9092"
      KAFKA_TOPIC: "user-events"
      KAFKA_CONSUMER_GROUP: "notification-service"
      MAILER_DSN: "smtp://mailhog:1025"
      MAILER_FROM: "no-reply@user-platform.local"
    restart: unless-stopped

  audit-service:
    build: ./audit-service
    image: ahceneaiti/audit-service:latest
    depends_on:
      kafka:
        condition: service_started
    environment:
      APP_ENV: prod
      APP_DEBUG: "0"
      APP_SECRET: local-dev-not-secret
      KAFKA_BROKERS: "kafka:9092"
      KAFKA_TOPIC: "user-events"
      KAFKA_CONSUMER_GROUP: "audit-service"
    restart: unless-stopped

volumes:
  pgdata:
  kafkadata:
